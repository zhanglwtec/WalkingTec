@model GitTest.ViewModel.HomeVMs.ReturnPasswordVM

@(
    UI.Form().Size(500,null).Items(
        UI.TextBox(x=>x.loginName)
    )
    .Buttons(
        UI.SubmitButton(),
        UI.CloseButton()
    ).RenderHtml()
)