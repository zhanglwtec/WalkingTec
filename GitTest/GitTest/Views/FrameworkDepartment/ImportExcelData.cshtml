@model GitTest.ViewModel.Framework.FrameworkDepartmentVMs.FrameworkDepartmentImportVM

@(
 UI.Container().VBoxLayout().Items(
    UI.Form().Size(550, null).Items(
        UI.Text(Language.BatchImport),
        UI.Container().HBoxLayout().Items(
            UI.DownloadTemplateButton(),
            UI.Text("    ").Size(10, null),
            UI.Upload(null).Required().Flex(1)
        ),
        UI.SimpleGrid(x => x.ErrorListVM).Flex(1)
    )
    .Buttons(
        UI.SubmitButton().ButtonText(Language.BatchImport),
        UI.CloseButton()
    )
 ).RenderHtml()
)


