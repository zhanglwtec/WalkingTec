@model GitTest.ViewModel.Framework.FrameworkCompanyVMs.FrameworkCompanyBatchVM

@(
 UI.Form().Size(750, 400).Items(
        UI.Display().Value(Language.DeleteConfirm),
        UI.SimpleGrid(x => x.ListVM),
        UI.Hidden(x => x.IDs)
    ).Buttons(
        UI.SubmitButton().ButtonText(Language.Delete),
        UI.CloseButton()
    ).RenderHtml()
)